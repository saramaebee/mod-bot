import { Action } from "../types";

class DbService {
	public static async submitToDb(action: Action): Promise<void> {
		return;
	}

	public static async retrieveFromDb(action: Action): Promise<void> {
		return;
	}
}